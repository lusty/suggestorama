<h1>Listing users</h1>

<table>
  <tr>
    <th>First</th>
    <th>Last</th>
    <th>Email</th>
    <th>Street</th>
    <th>City</th>
    <th>Zip</th>
    <th>Dayphone</th>
    <th>Evephone</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%=h user.first %></td>
    <td><%=h user.last %></td>
    <td><%=h user.email %></td>
    <td><%=h user.street %></td>
    <td><%=h user.city %></td>
    <td><%=h user.zip %></td>
    <td><%=h user.dayphone %></td>
    <td><%=h user.evephone %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>